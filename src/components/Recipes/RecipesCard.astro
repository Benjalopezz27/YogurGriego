---
interface Props {
  title: string;
  description: string;
  imageUrl: string;
  link: string;
}
const { title, description, imageUrl, link }: Props = Astro.props;
---

<div class="bg-white rounded-xl shadow-md overflow-hidden flex flex-col">
  <!-- Imagen -->
  <div class="overflow-hidden">
    <img
      src={imageUrl}
      alt={title}
      class="rounded-t-lg object-cover transform hover:scale-110 transition-transform duration-500 ease-in-out h-50 w-full"
      loading="lazy"
      decoding="async"
    />
  </div>
  <!-- Contenido -->
  <div class="p-4 flex flex-col flex-grow">
    <h3 class="text-lg font-semibold text-gray-800 mb-2">{title}</h3>
    <p class="text-gray-600 text-sm flex-grow">{description}</p>
    <a
      href={link}
      target="_blank"
      rel="noopener noreferrer"
      class="mt-4 inline-block bg-red-400 hover:bg-red-500 text-white text-sm font-medium px-4 py-2 rounded-lg transition-colors duration-300 self-start"
    >
      Ver más
    </a>
  </div>
</div>
